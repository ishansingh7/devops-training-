pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh '''
          echo "Starting build..."
          echo "hello"
          # other build commands
        '''
      }
    }
  }
}
